# UI Snapshots Directory

This directory is the storage location for visual regression snapshots. Once a UI view is finalized and approved, its snapshot should be documented here. Agents must verify changes against these approved snapshots to prevent UI drift and layout breaking.
